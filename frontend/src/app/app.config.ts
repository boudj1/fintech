/**
 * Legacy standalone configuration — kept for reference only.
 * The application now uses NgModule (AppModule) bootstrapped in main.ts.
 * This file is NOT imported anywhere and has no effect at runtime.
 */
export const appConfig = {};
