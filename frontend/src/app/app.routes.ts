/**
 * Legacy standalone routes — kept for reference only.
 * The application now uses AppRoutingModule (app-routing.module.ts) with NgModule.
 * This file is NOT imported anywhere and has no effect at runtime.
 */
import { Routes } from '@angular/router';

export const routes: Routes = [];
