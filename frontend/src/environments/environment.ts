export const environment = {
  production: false,
  // Use relative URL so the Angular proxy (proxy.conf.json) forwards /api → localhost:8080
  apiUrl: '/api',
  chatbotUrl: 'http://localhost:8001'
};
